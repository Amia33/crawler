query AvSearch(
  $limit: Int!
  $offset: Int
  $floor: PPVFloor = AV
  $sort: ContentSearchPPVSort = SALES_RANK_SCORE
  $queryWord: String
  $filter: ContentSearchPPVFilterInput
  $facetLimit: Int = 1
  $excludeUndelivered: Boolean = true
) {
  legacySearchPPV(
    limit: $limit
    offset: $offset
    floor: $floor
    sort: $sort
    queryWord: $queryWord
    filter: $filter
    facetLimit: $facetLimit
    includeExplicit: true
    excludeUndelivered: $excludeUndelivered
  ) {
    result {
      contents {
        id
        deliveryStartAt
      }
      pageInfo {
        offset
        limit
        hasNext
      }
    }
  }
}
