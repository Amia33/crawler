query AmateurSearch($offset: Int, $filter: ContentSearchPPVFilterInput) {
  graphql: legacySearchPPV(
    limit: 120
    offset: $offset
    floor: AMATEUR
    sort: DELIVERY_START_DATE
    filter: $filter
    includeExplicit: true
  ) {
    result {
      contents {
        id
      }
      pageInfo {
        hasNext
      }
    }
  }
}
