query Maker($offset: Int, $floor: Floor!) {
  graphql: makers(limit: 500, offset: $offset, floor: $floor, sort: NAME_ASC) {
    items {
      id
      name
      imageUrl
      description
      isExclusive
    }
    pageInfo {
      hasNext
    }
  }
}
